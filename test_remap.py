import math
import numpy as np

def remap(inR, X, Y):
    # This is a simplification of Android's remapCoordinateSystem
    # X and Y are the indices of the axes (1=X, 2=Y, 3=Z, 129=-X, 130=-Y, 131=-Z)
    
    # Let's map X=1, Y=3 (AXIS_X, AXIS_Z)
    pass

